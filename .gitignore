# кэши рыночных данных (скачиваются заново при первом запуске скриптов)
history_*.json
funding_*.json
oi_*.json
daily_pct5.json
fng_history.json
webapp/cache_*.json

# логи ботов/помощника/сервера
*.log
webapp/srv*.log

# новостной фон: локальное состояние, наполняется Claude через MCP
news_state.json
webapp/data/signals.json

# боевое состояние ботов (капитал, пауза kill switch, открытый цикл).
# Это снимок ЖИВЫХ денег конкретной машины — в общий репозиторий не место,
# иначе чужое состояние затрёт своё при обновлении.
state/
botstate_*.json
*.tmp

# временные выводы смоук-тестов
o_*.txt
e_*.txt
o1.txt
o2.txt
o3.txt
e1.txt
e2.txt
e3.txt
adv_o.txt
adv_e.txt
out.txt
err.txt
out2.txt
err2.txt

# python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# ключи никогда не хранятся в файлах (только переменные окружения),
# но на всякий случай:
.env
.env.*
*.key
*.pem
secrets*.json
api_key*.json

# локальные окружения и мусор редакторов/ОС
.venv/
venv/
env/
.idea/
.vscode/
.claude/
Thumbs.db
desktop.ini
.DS_Store
*.bak
*.orig
*.rej

# сырые свечи исследования — качаются fetch_ohlcv.py, в репозиторий не кладём
research/data/
research/out/
research/data_wide/
